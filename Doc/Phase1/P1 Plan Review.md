**⚠️ 需要补充或调整的地方**

**1. P1-1 图表数据来源需要确认**

计划写"调用 `getDashboardStats`"来渲染近 7 天订单趋势折线图，但现在 `getDashboardStats` 返回的是汇总数字（today/month），**没有按天分组的时序数据**。

折线图需要的结构是：

```
[{ date: "03-18", orders: 3 }, { date: "03-19", orders: 5 }, ...]
```

建议两个选项：

- **方案 A**：后端新增 `GET /api/dashboard/chart-data` 接口，返回近 7 天按日聚合数据，Mock 里造好假数据
- **方案 B**：前端从 `/api/orders` 拉近 7 天数据，在客户端聚合

推荐方案 A，逻辑在后端，前端干净。P1 计划里需要补这个后端任务。

---

**2. P1-5 数据库迁移顺序有风险**

计划把数据库迁移放在"Phase 1 结束前"，但如果 P1-2/P1-3 API 验证都依赖 Mock，**切换 `USE_MOCK=false` 时可能发现 Prisma 查询逻辑有 bug**，离 Phase 1 结束太近了，没有时间修。

建议调整：**P1-5 提前到 P1-2 开始之前做**，迁移完成后先用真实数据库跑一遍现有 Mock 路由，确认 Prisma Client 生成正常，再开始写库存/订单页。

---

**3. 订单详情 Drawer 缺少一个字段说明**

P1-3 写"展示商品明细 + 物流追踪号"，但现在 `Order` 表和 `OrderItem` 表里**没有物流追踪号字段**（`trackingNumber`）。

两个选择：

- 现在加字段到 Schema（需要新 migration）
- Drawer 里先不展示，等 SP-API 真实对接时再补

建议明确写进计划，避免 Agent 自己发明字段。

---

**4. 分工建议可以更精确**

当前写"前端工程师-P1 负责 P1-1+P1-2+P1-3+P1-4"，任务量偏重。P1-1 图表如果补了后端接口，可以让后端工程师顺手做，两个 Agent 并行更均衡：

|Agent|负责|
|---|---|
|前端工程师-P1|P1-4 工程配置 → P1-2 库存页前端 → P1-3 订单页前端|
|后端工程师-P1|P1-5 数据库迁移 → P1-1 新增图表数据接口 → P1-2/P1-3 API 验证|
|Dashboard 图表（可合并给前端）|P1-1 图表渲染，等后端接口就绪后接入|

---

**总结：3 个需要在计划里修订的点**

1. **补充** `GET /api/dashboard/chart-data` 后端接口任务到 P1-5 或新建 P1-6
2. **调整** P1-5 数据库迁移时机，提前到 P1-2 之前
3. **明确** 物流追踪号字段的处理方式（加 Schema 还是暂不展示）